using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace NPC
{
    [CreateAssetMenu(fileName = "过渡组", menuName = "角色/过渡/过渡组")]
    public class TransitionGroup : BaseTransition
    {

        public override bool Reason(BehaviorStateMachine user)
        {
            
        }
    }
}
